import React from "react";
import Register from "components/app-authorization/authorization-register/register";

const RegisterPage: React.FC = () => {
  return <Register />;
};

export default RegisterPage;
