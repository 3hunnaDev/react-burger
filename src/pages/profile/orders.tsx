import React from "react";
import ProfileOrders from "components/app-authorization/authorization-profile/profile-orders";

const ProfileOrdersPage: React.FC = () => {
  return <ProfileOrders />;
};

export default ProfileOrdersPage;
