import React from "react";
import Profile from "components/app-authorization/authorization-profile/profile";

const ProfilePage: React.FC = () => {
  return <Profile />;
};

export default ProfilePage;
