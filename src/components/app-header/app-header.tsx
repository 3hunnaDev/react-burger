import React from "react";
import appHeaderStyles from "./app-header.module.css";

const AppHeader: React.FC = () => {
  return <header className={appHeaderStyles.header} />;
};

export default AppHeader;
